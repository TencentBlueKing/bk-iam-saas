### 超管获取用户组成员列表

> 超级管理员获取指定用户组的成员列表


#### Parameters

| 字段 |  类型 |是否必须  | 位置 |描述  |
|--------|--------|--------|--------|--------|
| id | int | 是 | path | 用户组ID |
| page_size |  int  | 否| query | 分页查询参数之一，page_size 表示查询数量，`最大值为1000`，默认为10 |
| page  | int | 否| query | 分页查询参数之一，page 表示从第几页开始查询，page 从 1 开始计算 |
| limit |  int  | 否| query | 分页查询参数之一，limit 表示查询数量，与 page_size 等效 |
| offset  | int | 否| query | 分页查询参数之一，offset 表示偏移量，与 (page-1)*page_size 等效 |

#### Response

| 字段      | 类型      | 描述      |
|-----------|-----------|-----------|
| count   | int     |  总数量 |
| results   |  array[object]   |  分页查询到结果 |

results
| 字段      | 类型      | 描述      |
|-----------|-----------|-----------|
| type | string | 成员类型，user表示用户，department表示部门 |
| id | string | 成员ID，用户ID或部门ID |
| name | string | 成员名称，用户名或部门名称 |
| expired_at | int | 过期时间，Unix时间戳，-1表示永久 |


```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "count": 2,
    "results": [
      {
        "type": "user",
        "id": "admin",
        "name": "admin",
        "expired_at": 1619596800
      },
      {
        "type": "department",
        "id": "1",
        "name": "研发部门",
        "expired_at": -1
      }
    ]
  }
}
```