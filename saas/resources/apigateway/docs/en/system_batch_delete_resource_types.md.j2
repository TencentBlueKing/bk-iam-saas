### Batch delete resourceType

----------
{% include '_api_v1_model_resource_types_delete.md.j2' %}
----------

#### Parameters

| field | type | required | location | description |
| :--- | :---| :--- |:--- |:--- |
| system_id | string | yes | path | system_id |
| check_existence | boolean | no | query | Default will check if id exists, if it doesn't, the delete will fail, set false to not check if id exists, go straight to delete (`delete from table where id in []`) |

#### parameters

| field | type   | required | description |
|-------|--------|----------|-------------|
| id    | string | yes      | resourceType id |

#### Request

```json
[
    {
        "id": "biz"
    },
    {
        "id": "set"
    }
]
```

#### Response

```json
{
    "code": 0,
    "message": "",
    "data": {}
}
```
